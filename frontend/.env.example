# Frontend Environment Variables Template
# Copy this file to .env and fill in your actual values

# Backend API URL
# The base URL of your backend API server
# Used by the Nuxt development server to proxy /api/* requests

# For local development (when backend is running locally):
API_URL=http://localhost:3000

# For Docker Compose (when running with docker compose up):
# API_URL=http://backend:3000
